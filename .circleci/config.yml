version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:7.10

    working_directory: ~/repo

    steps:
      - checkout
      - run: yarn test
notify:
   webhooks:
     - url: https://hooks-glpciaws.dev.glip.net/webhook/c8bcbb2a-b4dc-40c2-adea-1073d1899e51
